---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - app/layout.tsx
  - app/page.tsx
  - lib/supabase.ts
  - .env.local
  - .gitignore
autonomous: false

user_setup:
  - service: supabase
    why: "Database and authentication backend"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon public key"
    dashboard_config:
      - task: "Create new Supabase project"
        location: "https://supabase.com/dashboard -> New Project"

must_haves:
  truths:
    - "Next.js app runs locally on localhost:3000"
    - "App can query Supabase database from serverless API route"
    - "TypeScript compilation succeeds without errors"
  artifacts:
    - path: "package.json"
      provides: "Dependencies including Next.js, React, Supabase client"
      contains: "next"
    - path: "lib/supabase.ts"
      provides: "Supabase client factory"
      exports: ["createClient"]
    - path: "app/api/health/route.ts"
      provides: "Health check endpoint that tests Supabase connection"
      exports: ["GET"]
  key_links:
    - from: "app/api/health/route.ts"
      to: "lib/supabase.ts"
      via: "import and call createClient"
      pattern: "createClient\\(\\)"
    - from: "lib/supabase.ts"
      to: "process.env.NEXT_PUBLIC_SUPABASE_URL"
      via: "environment variable"
      pattern: "process\\.env\\.NEXT_PUBLIC_SUPABASE"
---

<objective>
Initialize Next.js application with TypeScript and connect to Supabase database.

Purpose: Establishes the foundational web framework and database connection that all subsequent features will build upon.

Output: Working Next.js app with Supabase client configured, running locally and ready for deployment.
</objective>

<execution_context>
@C:\Users\aryan\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\aryan\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Initialize Next.js project with TypeScript and essential dependencies</name>
  <files>
package.json
next.config.ts
tsconfig.json
app/layout.tsx
app/page.tsx
.gitignore
  </files>
  <action>
Create a new Next.js 14 project using create-next-app with the following configuration:
- TypeScript enabled
- App Router (not Pages Router)
- Tailwind CSS for styling
- ESLint for code quality
- src/ directory: NO (use app/ directly)

Run: npx create-next-app@latest . --typescript --app --tailwind --eslint --no-src-dir --import-alias "@/*"

Then install additional dependencies:
- @supabase/supabase-js (Supabase client)
- @supabase/ssr (for Next.js integration)

Create a minimal homepage (app/page.tsx) that displays "type-it-up" heading and brief description from PROJECT.md.

Update .gitignore to include:
- .env.local
- .env*.local
- .vercel
  </action>
  <verify>
Run `npm run dev` and confirm app loads at http://localhost:3000 with no errors. TypeScript compilation succeeds with `npx tsc --noEmit`.
  </verify>
  <done>
Next.js app runs locally, shows homepage content, TypeScript compiles without errors, all dependencies installed.
  </done>
</task>

<task type="auto">
  <name>Configure Supabase client connection</name>
  <files>
lib/supabase.ts
app/api/health/route.ts
.env.local.example
  </files>
  <action>
Create lib/supabase.ts that exports a createClient function using @supabase/ssr for Next.js App Router. Follow the pattern from Supabase docs for server components and route handlers.

The client should read from environment variables:
- NEXT_PUBLIC_SUPABASE_URL
- NEXT_PUBLIC_SUPABASE_ANON_KEY

Create app/api/health/route.ts that:
1. Imports createClient from lib/supabase
2. Attempts a simple query: `SELECT NOW()` to verify database connectivity
3. Returns JSON: {status: "ok", database: "connected", timestamp: ...} on success
4. Returns JSON: {status: "error", message: ...} with 500 status on failure

Create .env.local.example with placeholder values showing required env vars (DO NOT include actual secrets).

DO NOT create .env.local with real values - user will provide credentials in next task checkpoint.
  </action>
  <verify>
After user provides credentials in checkpoint, run `curl http://localhost:3000/api/health` and verify it returns {status: "ok", database: "connected"}.
  </verify>
  <done>
lib/supabase.ts exports working client factory, health endpoint successfully queries Supabase and returns connection status.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Set up Supabase project and provide credentials</action>
  <instructions>
1. Visit https://supabase.com/dashboard
2. Sign in or create account (free tier)
3. Click "New Project"
4. Fill in:
   - Name: type-it-up
   - Database Password: (generate strong password, save it securely)
   - Region: (choose closest to you)
5. Wait for project to finish provisioning (2-3 minutes)
6. Go to Project Settings -> API
7. Copy "Project URL" and "anon public" key
8. Create .env.local in project root with:

```
NEXT_PUBLIC_SUPABASE_URL=your_project_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
```

9. Restart dev server: `npm run dev`
10. Test: `curl http://localhost:3000/api/health`
11. Verify response shows {status: "ok", database: "connected"}
  </instructions>
  <resume-signal>Type "connected" when health check returns ok status</resume-signal>
</task>

</tasks>

<verification>
1. Next.js dev server runs without errors
2. Homepage displays project branding
3. TypeScript compilation succeeds
4. Supabase health endpoint returns successful connection status
5. .env.local exists with valid credentials (not committed to git)
</verification>

<success_criteria>
- Next.js app runs locally on localhost:3000
- App can query Supabase database from API route
- Health endpoint confirms database connectivity
- TypeScript has no compilation errors
- Environment variables properly configured
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` documenting:
- Next.js version and configuration choices
- Supabase client setup pattern
- Health check endpoint behavior
- Environment variables required
</output>
